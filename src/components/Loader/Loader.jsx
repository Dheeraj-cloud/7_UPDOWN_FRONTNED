import React from "react";
import { CircularProgress } from "@mui/material";

const Loader = () => {
  return (
    <div className={root}>
      <CircularProgress />
    </div>
  );
};

export default Loader;
